import { ProtocolEntryTimePipe } from './protocol-entry-time.pipe';

// TODO: test ProtocolEntryTimePipe
describe('ProtocolEntryTimePipe', () => {
	it('create an instance', () => {
		const pipe = new ProtocolEntryTimePipe();
		expect(pipe).toBeTruthy();
	});
});
